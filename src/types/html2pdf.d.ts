// This file tells TypeScript that the 'html2pdf.js' module exists,
// even though it doesn't have its own type definitions.
// This prevents errors when importing it in other files.
declare module 'html2pdf.js';
